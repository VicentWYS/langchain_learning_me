## 配置基本环境
1. vscode项目中打开终端，确认是当前根目录，执行：`python -m venv venv`;
2. 激活虚拟环境：执行 `venv/Scripts/activate`, 成功后，终端前面会变成类似：`(venv) PS D:\your_project>`;
3. 选择解释器：vscode中，`Ctrl + Shift + P` -> `Python: Select Interpreter` -> 选择类似带venv的 `Python 3.x.x ('venv': venv)`
